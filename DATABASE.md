
The docker-compose located in the database directory uses a database called "first" by default, but the actual projects use the "dvdrental" database commonly used in postgres tutorials

Create it yourself for eternal joy
